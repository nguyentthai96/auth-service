<?xml version="1.0" encoding="UTF-8"?>
<entity-mappings xmlns="https://jakarta.ee/xml/ns/persistence/orm"
                 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                 xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence/orm
                 https://jakarta.ee/xml/ns/persistence/orm/orm_3_2.xsd"
                 version="3.2">

    <!--
      Global entity listener workaround for Hibernate 7.4.x @IdGeneratorType issue.
      Assigns Snowflake IDs via @PrePersist when @SnowflakeId annotation
      is not properly resolved from inherited MappedSuperclass fields.
    -->
    <persistence-unit-metadata>
        <persistence-unit-defaults>
            <entity-listeners>
                <entity-listener class="com.ntt.authservice.shared.persistence.SnowflakeIdAssignListener"/>
            </entity-listeners>
        </persistence-unit-defaults>
    </persistence-unit-metadata>

</entity-mappings>
