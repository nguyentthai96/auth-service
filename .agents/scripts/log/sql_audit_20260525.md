# SQLcl MCP Audit Log
# Generated: 2026-05-25T19:20:00+07:00
# Database: EBANKBIDC_CAM@10.22.19.128:1521:DVNHT1
# Purpose: Ghi lại toàn bộ SQL queries đã thực hiện qua MCP để khám phá schema database

## Session: Schema Discovery (2026-05-25)

### 1. Connection Setup
```sql
-- Tool: mcp_sqlcl_connections_list
-- Mục đích: Liệt kê connections đã lưu
-- Kết quả: Tìm thấy connection "BIDC_DEV"
```

```sql
-- Tool: mcp_sqlcl_connect
-- Connection: BIDC_DEV
-- Kết quả: Connected to EBANKBIDC_CAM@jdbc:oracle:thin:@10.22.19.128:1521:DVNHT1 (Oracle 19c)
```

### 2. Schema Overview
```sql
-- Tool: mcp_sqlcl_schema_information (level=BRIEF)
-- Mục đích: Lấy danh sách tất cả objects trong schema
-- Kết quả: 252 tables, 594+ rows metadata
```

```sql
-- Tool: mcp_sqlcl_sql_run
SELECT COUNT(*) AS total_tables FROM user_tables;
-- Kết quả: 252
```

### 3. AD_ Tables - Administration Domain
```sql
-- Tool: mcp_sqlcl_schema_information (level=DETAILED, filter=AD_%)
-- Mục đích: Cấu trúc chi tiết toàn bộ bảng AD_*
-- Kết quả: 594 rows covering ~60 tables
-- Bảng chính: AD_CONFIG, AD_BANK, AD_BRANCH, AD_CITY, AD_CURRENCY,
--   AD_PACKAGES, AD_PKG_SERVICE, AD_PKG_LIMIT, AD_PRODUCTS,
--   AD_BILL_SERVICES, AD_BILL_PROVIDER, AD_USER, AD_ROLE,
--   AD_SMS_TEMPLATE, AD_ERRORMSG, AD_ALLCODE, AD_PAIR_KEY, AD_PAIR_EAV
```

### 4. MB_ Tables - Mobile Banking Core Domain
```sql
-- Tool: mcp_sqlcl_schema_information (level=DETAILED, filter=MB_%)
-- Mục đích: Cấu trúc chi tiết toàn bộ bảng MB_*
-- Kết quả: 1,014+ rows covering ~55 tables
-- Bảng chính: MB_CUSTOMER (51 cols), MB_ACCOUNTS (23 cols),
--   MB_MOBILES (45 cols), MB_TRANSACTION (80 cols), MB_MID (9 cols),
--   MB_DEVICE_SESSION, MB_DEVICE_CLIENT, MB_DEVICE_ACCESS,
--   MB_OTP, MB_CLIENT, MB_LOGS, MB_EBANKLOGS, MB_FEE_DEBT,
--   MB_KEYS, MB_RSA_KEY, MB_SERVICE, MB_MOBILE_ACCOUNT,
--   MB_SAVING_DETAIL, MB_PIN_CODE_HIS, MB_PIN_PAY_HIS,
--   MB_OPEN_ACCOUNT, MB_CASH_MONEY_BIDV, MB_QUERY_BILL
```

### 5. Other Tables - IBB, GW, EKYC, Bakong, etc.
```sql
-- Tool: mcp_sqlcl_schema_information (level=DETAILED)
-- Filter: NOT LIKE 'AD_%' AND NOT LIKE 'MB_%'
-- Mục đích: Cấu trúc chi tiết các bảng còn lại
-- Kết quả: 1,597 rows covering remaining tables

-- IBB_* Tables (Internet Banking Business):
--   IBB_CUSTOMERS (31 cols), IBB_USERS (27 cols), IBB_ACCOUNTS (22 cols),
--   IBB_ACCOUNTS_USERS (12 cols), IBB_TRANSACTIONS (56 cols),
--   IBB_TRANSFER_BATCH (25 cols), IBB_PACKAGES, IBB_SERVICES, IBB_LOGS

-- BAKONG Tables:
--   BAKONG_INCOMING_DEFAULT_ACCOUNT (10 cols) - QR default account

-- EKYC Tables:
--   EKYC_REGISTER (41 cols), EKYC_REGISTER_STEP

-- GW_* Tables (Gateway):
--   GW_PARTNERS, GW_PARTNER_API_ACCESS, GW_CUSTOMERS_REGISTER, GW_SMS_LOG

-- Other:
--   BAL_CHG_TRANX, FEE_TRANSACTION_SMS, FEE_TRANSFER_SMS,
--   OTP_SMS, SMS_LOG, SMS_MESSAGES, SYSTEM_FEE, EMAIL_REPORT
```

### 6. Bakong QR Default Account (Focus Query)
```sql
-- Tool: mcp_sqlcl_schema_information (level=DETAILED, objects_list=BAKONG_INCOMING_DEFAULT_ACCOUNT)
-- Mục đích: Chi tiết bảng mới cho tính năng QR Default Account
-- Kết quả:
--   ID                    NUMBER(19,0) PK NOT NULL
--   CIF                   VARCHAR2(20)    NOT NULL
--   USERNAME              VARCHAR2(50)    NOT NULL
--   CUSTOMER_NAME         VARCHAR2(100)   NULLABLE
--   BRANCH_CODE           VARCHAR2(10)    NULLABLE
--   DEFAULT_KHR_ACCOUNT   VARCHAR2(30)    NOT NULL
--   DEFAULT_USD_ACCOUNT   VARCHAR2(30)    NOT NULL
--   STATUS                NUMBER(1,0)     NOT NULL
--   CREATED_AT            TIMESTAMP(6)    NOT NULL
--   UPDATED_AT            TIMESTAMP(6)    NULLABLE
```

### 7. Test Queries
```sql
-- Tool: mcp_sqlcl_sql_run
SELECT 'PROXY_LOGGER_TEST' AS test_label, COUNT(*) AS total_tables FROM user_tables;
-- Kết quả: PROXY_LOGGER_TEST, 252

-- Tool: mcp_sqlcl_sql_run
SELECT 'LOG_TEST_V2' AS test_label, SYSDATE AS current_time FROM DUAL;
-- Kết quả: LOG_TEST_V2, 25-MAY-26
```

---

---

## Session: PL/SQL Source Code Extraction (2026-05-25 19:22)

### 8. List all PL/SQL Objects
```sql
-- Tool: mcp_sqlcl_sql_run
SELECT object_type, object_name, status, created, last_ddl_time
FROM user_objects
WHERE object_type IN ('PACKAGE', 'PACKAGE BODY', 'PROCEDURE', 'FUNCTION')
ORDER BY object_type, object_name;
-- Kết quả: 134 objects (5 FUNCTION, 63 PACKAGE, 63 PACKAGE BODY, 3 PROCEDURE)
```

### 9. Extract Package Specs Source
```sql
-- Tool: mcp_sqlcl_sql_run
SELECT name, type, line, text FROM user_source
WHERE type = 'PACKAGE' ORDER BY name, line;
-- Kết quả: 16,949 lines → saved to step 250/output.txt
```

### 10. Extract Functions Source
```sql
-- Tool: mcp_sqlcl_sql_run
SELECT name, type, line, text FROM user_source
WHERE type = 'FUNCTION' ORDER BY name, line;
-- Kết quả: 207 lines → saved to step 251/output.txt
```

### 11. Extract Procedures Source
```sql
-- Tool: mcp_sqlcl_sql_run
SELECT name, type, line, text FROM user_source
WHERE type = 'PROCEDURE' ORDER BY name, line;
-- Kết quả: 320 lines → saved to step 252/output.txt
```

### 12. Extract Package Bodies Source (ASYNC)
```sql
-- Tool: mcp_sqlcl_sql_run (execution_type=ASYNCHRONOUS)
SELECT name, type, line, text FROM user_source
WHERE type = 'PACKAGE BODY' ORDER BY name, line;
-- Kết quả: 75,335 lines → saved to step 261/output.txt
```

### 13. Parse & Generate SQL Files
```
-- Tool: run_command
python3 .agent/scripts/parse_plsql_source.py
-- Kết quả: 134 SQL files generated in docs/plsql/
--   packages/      63 files (532K)
--   package_bodies/ 63 files (1.6M)
--   functions/      5 files (24K)
--   procedures/     3 files (20K)
--   INDEX.md        mapping index
-- Total: 2.2M
```

### Top 10 Largest Package Bodies (by lines)
| Package | Lines |
|---------|-------|
| PKG_IBB_BIDC | 4,030 |
| PKG_MB_BIDC | 2,919 |
| PKG_PUBLICINFO | 2,535 |
| PKG_SMSBANKINGCORE | 2,196 |
| PKG_REGISTRATION | 2,069 |
| PKG_CUSTOMER | 1,892 |
| PKG_IBB_REGISTRATION | 1,881 |
| PKG_REPORTS_TRANSACTION | 1,827 |
| PGK_IBB_CUSTOMER | 1,710 |
| PKG_WEBMANAGEMENT | 1,600 |

---

## Notes
- Proxy logger (`sqlcl-mcp-logger.py`) captures MCP startup/shutdown nhưng Antigravity reuse MCP process nên không log được toàn bộ session
- File này được tạo thủ công từ agent để đảm bảo audit trail đầy đủ
- Output schema đã được tổng hợp tại `docs/database-schema.md`
- PL/SQL source code đã được lưu tại `docs/plsql/` với mapping index tại `docs/plsql/INDEX.md`
